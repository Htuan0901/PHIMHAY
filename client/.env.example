# Copy thành client/.env — chỉ biến Vite (prefix VITE_)
# Local: để trống để dùng proxy /api trong vite.config
# Render single service: để trống (gọi /api cùng domain)
# Chỉ set khi FE deploy tách khỏi API:
# VITE_API_URL=https://your-api.example.com
